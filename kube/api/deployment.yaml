kind: Deployment
apiVersion: apps/v1
metadata:
  name: social-albums-api
  namespace: social-albums
  labels:
    app: social-albums-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: social-albums-api
  template:
    metadata:
      labels:
        app: social-albums-api
    spec:
      containers:
      - envFrom:
          - secretRef:
              name: social-albums-api-secrets
        image: angelosnm/albums-api:58174ab-33
        name: social-albums-api
      initContainers:
        - name: wait-for-mysql-pod
          image: busybox:1.28
          args:
            - /bin/sh
            - -c
            - until telnet social-albums-mysql 3306; do echo waiting for mysql-service; sleep 2; done;