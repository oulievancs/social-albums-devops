apiVersion: v1
kind: Service
metadata:
  name: social-users-producer
  namespace: social-albums
spec:
  selector:
    app: social-users-producer
  ports:
    - targetPort: 5000
      port: 5000
  type: ClusterIP
